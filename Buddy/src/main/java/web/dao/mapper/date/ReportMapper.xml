<?xml version="1.0" encoding="UTF-8"?>


<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="web.dao.face.adopt.ReportDao">

	<select id="reptSelectCntAll" resultType="int">
		SELECT count(*) FROM report
	</select>

	<select id="reptSelectList" resultType="Report" parameterType="Paging">
		SELECT * FROM (
			SELECT rownum rnum, R.* FROM (
				SELECT
					rept_no, adminid, rept_title, rept_content
					, rept_date
				FROM report
				ORDER BY rept_no DESC
			) R
		) REPORT
		WHERE rnum BETWEEN #{startNo } AND #{endNo }
	</select>


</mapper>